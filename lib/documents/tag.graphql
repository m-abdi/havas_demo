query getTagData($tagId: ID!) {
  tagData(tagId: $tagId) {
    id
    asset {
      id
      equipment {
        terminologyCode
      }
    }
  }
}

mutation createTags($tags: [NewTag!]!) {
  createTags(tags: $tags)
}
